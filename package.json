{
  "name": "loi-smoke-tests",
  "version": "1.0.0",
  "scripts": {
    "test": "jest --detectOpenHandles"
  },
  "devDependencies": {
    "jest": "^27.0.6",
    "jest-puppeteer": "^5.0.4",
    "puppeteer": "^10.1.0"
  },
  "jest": {
    "verbose": true,
    "preset": "jest-puppeteer"
  }
}
